# An unknown Host header falls through to the engine's default host, then to
# the adapter's app_base; nothing is found and we get a 404.
status: 404
header.content-type: text/html; charset=utf-8
present.server:
present.content-length:
body.contains: 404
body.contains: Not Found
body.len_ge: 100
