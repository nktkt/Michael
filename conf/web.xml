<?xml version="1.0" encoding="UTF-8"?>
<!--
  Minimal sample web.xml for the Tomcat-RS Compatibility Runtime.

  Uses the Jakarta web-app 6.0 namespace (the level shipped with Tomcat 11).
  This descriptor is parsed by tomcatrs-config; servlet execution itself runs
  on the embedded JVM via tomcatrs-servlet-bridge.
-->
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

  <display-name>Tomcat-RS Sample Web Application</display-name>

  <!-- A single servlet definition. -->
  <servlet>
    <servlet-name>hello</servlet-name>
    <servlet-class>com.example.HelloServlet</servlet-class>
    <load-on-startup>1</load-on-startup>
  </servlet>

  <!-- Map the servlet onto a URL pattern. -->
  <servlet-mapping>
    <servlet-name>hello</servlet-name>
    <url-pattern>/hello</url-pattern>
  </servlet-mapping>

  <!-- Files tried, in order, when a request targets a directory. -->
  <welcome-file-list>
    <welcome-file>index.html</welcome-file>
    <welcome-file>index.jsp</welcome-file>
  </welcome-file-list>

  <!-- Session timeout, in minutes. -->
  <session-config>
    <session-timeout>30</session-timeout>
  </session-config>

</web-app>
